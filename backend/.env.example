PORT=5000
MONGO_URI=mongodb://127.0.0.1:27017/student_performance_db
PYTHON_PATH=python
# Path is resolved from backend/ml-integration/ (default below reaches repo ml-model/)
ML_PREDICT_SCRIPT=../../ml-model/predict.py

# Required in production — use a long random string
JWT_SECRET=change-this-to-a-long-random-secret
# Optional (default 7d)
# JWT_EXPIRES_IN=7d
